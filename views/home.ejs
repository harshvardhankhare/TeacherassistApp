<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Teacher Assistant | Classroom Management</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.0/font/bootstrap-icons.css">
  <style>
    :root {
      --primary-color: #4e73df;
      --secondary-color: #1cc88a;
      --dark-color: #5a5c69;
      --light-color: #f8f9fc;
    }
    
    body {
      background-color: var(--light-color);
      color: var(--dark-color);
      min-height: 100vh;
      display: flex;
      flex-direction: column;
    }
    
    .hero-section {
      background: linear-gradient(135deg, var(--primary-color) 0%, #224abe 100%);
      color: white;
      border-radius: 0 0 1rem 1rem;
      box-shadow: 0 0.5rem 1rem rgba(0, 0, 0, 0.1);
    }
    
    .welcome-card {
      border: none;
      border-radius: 1rem;
      box-shadow: 0 0.5rem 1rem rgba(0, 0, 0, 0.1);
      transition: transform 0.3s ease;
    }
    
    .welcome-card:hover {
      transform: translateY(-5px);
    }
    
    .btn-primary {
      background-color: var(--primary-color);
      border-color: var(--primary-color);
      padding: 0.5rem 1.5rem;
    }
    
    .btn-outline-light {
      padding: 0.5rem 1.5rem;
    }
    
    .btn-success {
      background-color: var(--secondary-color);
      border-color: var(--secondary-color);
    }
    
    .feature-icon {
      font-size: 2.5rem;
      color: var(--primary-color);
      margin-bottom: 1rem;
    }
    
    .auth-links .btn {
      min-width: 120px;
    }
    
    footer {
      background-color: white;
      margin-top: auto;
    }
  </style>
</head>
<body>
  <% if (session && session.userId) { %>
    <!-- Logged In View -->
    <div class="hero-section py-5 mb-5">
      <div class="container text-center py-4">
        <h1 class="display-4 fw-bold mb-3">
          <i class="bi bi-speedometer2 me-2"></i>Welcome Back!
        </h1>
        <p class="lead mb-4">Ready to manage your classroom today?</p>
        <div class="d-flex justify-content-center gap-3">
          <a href="/students" class="btn btn-light btn-lg">
            <i class="bi bi-columns-gap me-2"></i>Go to Dashboard
          </a>
          <a href="/logout" class="btn btn-outline-light btn-lg">
            <i class="bi bi-box-arrow-right me-2"></i>Logout
          </a>
        </div>
      </div>
    </div>

    <div class="container mb-5">
      <div class="row g-4">
        <div class="col-md-4">
          <div class="welcome-card p-4 text-center h-100">
            <i class="bi bi-people feature-icon"></i>
            <h3>Student Management</h3>
            <p>View and manage all your student records in one place.</p>
            <a href="/students" class="btn btn-primary">Manage Students</a>
          </div>
        </div>
        <div class="col-md-4">
          <div class="welcome-card p-4 text-center h-100">
            <i class="bi bi-calendar-check feature-icon"></i>
            <h3>Attendance Tracking</h3>
            <p>Easily track and record student attendance.</p>
            <a href="/students" class="btn btn-primary">View Attendance</a>
          </div>
        </div>
        <div class="col-md-4">
          <div class="welcome-card p-4 text-center h-100">
            <i class="bi bi-cash-coin feature-icon"></i>
            <h3>Fee Management</h3>
            <p>Track fee payments and generate reports.</p>
            <a href="/students" class="btn btn-primary">View Fees</a>
          </div>
        </div>
      </div>
    </div>

  <% } else { %>
    <!-- Guest View -->
    <div class="hero-section py-5 mb-5">
      <div class="container text-center py-4">
        <h1 class="display-4 fw-bold mb-3">
          <i class="bi bi-mortarboard me-2"></i>Teacher's Assistant
        </h1>
        <p class="lead mb-4">Streamline your classroom management with our powerful tools</p>
        <div class="d-flex justify-content-center gap-3 auth-links">
          <a href="/login" class="btn btn-light btn-lg">
            <i class="bi bi-box-arrow-in-right me-2"></i>Login
          </a>
          <a href="/register" class="btn btn-outline-light btn-lg">
            <i class="bi bi-person-plus me-2"></i>Register
          </a>
        </div>
      </div>
    </div>

    <div class="container mb-5">
      <div class="row g-4">
        <div class="col-md-4">
          <div class="welcome-card p-4 text-center h-100">
            <i class="bi bi-people feature-icon"></i>
            <h3>Student Records</h3>
            <p>Comprehensive student profiles with all essential information in one place.</p>
          </div>
        </div>
        <div class="col-md-4">
          <div class="welcome-card p-4 text-center h-100">
            <i class="bi bi-calendar-check feature-icon"></i>
            <h3>Attendance Tracking</h3>
            <p>Simple and efficient system for tracking student attendance patterns.</p>
          </div>
        </div>
        <div class="col-md-4">
          <div class="welcome-card p-4 text-center h-100">
            <i class="bi bi-cash-coin feature-icon"></i>
            <h3>Fee Management</h3>
            <p>Easy tracking of payments with visual indicators and reporting.</p>
          </div>
        </div>
      </div>
    </div>
  <% } %>

  <footer class="py-4 mt-auto">
    <div class="container text-center">
      <p class="mb-0 text-muted">&copy; <%= new Date().getFullYear() %> Teacher Assistant App. All rights reserved.</p>
    </div>
  </footer>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>